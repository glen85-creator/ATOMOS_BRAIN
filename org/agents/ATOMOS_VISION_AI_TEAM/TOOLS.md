# TOOLS — ATOMOS_VISION_AI_TEAM

## 허용 toolsets
`terminal` 만 (현 단계) — 4단계 플레이북(이슈 fetch → 판독 결과 → JSON 코멘트 → done).
금지 목록은 ATOMOS_HERMES TOOLS.md와 동일.

## 비전 처리 (미구현)
이미지 분석·OCR 파이프라인 = capability 미구현 (개발 전 dormant 단계).
Gemini 3 Flash 비전 모델 연동은 파이프라인 구축 완료 후 적용. 현재 이미지 직접 처리 시도 금지.
toolsets 승격(vision/ocr toolset 추가)은 개발 완료 후 조직 결정으로 이루어진다.
